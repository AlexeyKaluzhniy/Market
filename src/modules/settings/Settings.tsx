import {NavigationFunctionComponent} from "react-native-navigation";
import {Text, View} from "react-native";

export const Settings: NavigationFunctionComponent = (props) => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
};
